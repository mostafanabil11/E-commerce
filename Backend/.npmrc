# The Nest CLI and TypeScript live in devDependencies but are required to build.
# Hosting platforms set NODE_ENV=production, which makes `npm install` prune dev
# dependencies and leaves `nest build` with no binary to run. Clearing `omit`
# keeps them installed at build time.
omit=
